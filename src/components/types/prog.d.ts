declare type TTextPosition = {
  text: string;
  y: number;
};

declare type TPhrase = TTextPosition[];

declare type TTextItems = {
  name: string;
  date: string;
  text: string;
  index: number;
};
